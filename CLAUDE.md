# CLAUDE.md
<!-- Template by EMUYN LLC — https://github.com/EMUYN-LLC/claude-code-paper-writing-template (CC BY 4.0) -->

このファイルは Claude Code がセッション開始時に常時読み込みます。
作業内容に応じて、サブディレクトリの CLAUDE.md も自動で追加読み込みされます。

- 文献作業: `references/CLAUDE.md`
- 原稿作業: `manuscripts/CLAUDE.md`
- スクリプト実行: `scripts/CLAUDE.md`

---

## 初回セットアップ

「プロジェクト概要」または「論文一覧」に `[要設定]` が残っている場合は、初回セットアップが必要。
**ユーザーが何かを言う前に**、Claude のほうから以下を順番に進めること:

まず挨拶と説明をする:
「論文執筆テンプレートへようこそ。セットアップを始めます。いくつか質問をします。」

次に以下を 1 項目ずつ質問し、すべて答えてもらったら各ファイルを更新する:
1. 研究テーマ・背景 (2〜3 文で教えてもらう) → このファイルの「プロジェクト概要」を書き換える
2. 論文一覧 (ID・テーマ・状態) → このファイルの「論文一覧」を書き換える
3. PubMed 検索キーワード (英語) → `scripts/collect_config.yaml` の `query` に書き込む
4. 候補取得数 (デフォルト 50) と処理件数上限 (デフォルト 20) → `scripts/collect_config.yaml` の `search_results` / `max_results` に書き込む
5. メールアドレス (API 識別用。登録不要) → `scripts/collect_config.yaml` の `email` に書き込む

すべて回答が揃ったら設定ファイルを更新する。その後、文献収集を開始する前に以下を順に実行する:

0. Windows の場合は `echo %TEMP%` を実行して実際の一時フォルダパスを取得し、`.claude/settings.json` の `additionalDirectories` にそのパスを追加する (エージェントが一時ファイルに書き込めるようにするため)

1. `python --version` または `python3 --version` を実行して Python が使えるか確認する
2. Python が見つからない場合はインストールを案内してから止まる:
   - Mac: `brew install python3` (Homebrew 未導入なら先にその手順を案内する)
   - Windows: Microsoft Store で「Python」を検索してインストールするよう案内する
3. Python が確認できたら `pip install -r scripts/requirements.txt` を実行する
4. 「自動収集しますか？」と 1 回だけ確認してから `python scripts/collect.py` を実行する
5. スクリプト完了後:
   - 手動入手が必要な論文がある場合: 「ブラウザでダウンロードページを開きました。PDF を references/pdfs/ に保存し終えたら教えてください」と伝えて待つ
   - すべて自動取得できた場合: 「二者協議 (統計家・専門医の 2 エージェントを並列起動) を実行しますか？」と確認し、承認されたら `prompts/02〜03` のエージェントを並列起動する
   - エージェント完了後は必ず以下の形式で次の作業を案内すること:

```
=== 二者協議 完了: PMID {PMID} ===

評価ファイル:
  統計家 → {TEMP}/eval_{PMID}_statistician.md
  専門医 → {TEMP}/eval_{PMID}_specialist.md

次の作業: 評価結果をサマリーに統合する
  ・サマリー未作成の場合は先に prompts/01_summary_creation.md のプロンプトで作成すること
  ・サマリー作成済みの場合は以下をそのまま送信:

──────────────────────────────────────────────────
{TEMP}/eval_{PMID}_statistician.md と {TEMP}/eval_{PMID}_specialist.md を読み、
references/summaries/ 内の該当サマリーの「二者協議による批判的評価」セクションに統合してください。
──────────────────────────────────────────────────
```

6. 手動保存完了の報告を受けたら:
   - `references/pdfs/` 内で `{数字のみ}.pdf` の命名規則に合わない PDF を見つける
   - 該当ファイルを読み、`references/abstracts/` 内のアブストラクトとタイトル・著者を照合して対応する PMID を特定する
   - 特定できたら `{PMID}.pdf` に改名する (確認不要)
   - `references/abstracts/` に存在する PMID のうち `references/pdfs/` に同名 PDF が追加されたものを特定する
   - PDF が追加されたものは全文で、まだない場合はアブストラクトで「二者協議 (統計家・専門医の 2 エージェントを並列起動) を実行しますか？」と確認し、承認されたら `prompts/02〜03` のエージェントを並列起動する
   - エージェント完了後は上記と同じ形式で次の作業を案内すること

---

## AI 利用に関する基本方針

このリポジトリは**研究者自身による論文執筆を支援するためのもの**です。
AI が生成したテキストをそのまま論文として投稿することは学術倫理違反です。
原稿は最終的に研究者自身が責任をもって確認・修正し、自分の言葉として成立させること。

---

## プロジェクト概要

[要設定: 研究プロジェクトの概要を 2〜3 文で記述する]

---

## 論文一覧

[要設定]

| ID | テーマ | 状態 | 原稿 |
|----|--------|------|------|
| P01 | [論文テーマ] | 執筆中 | `manuscripts/P01_your_study/` |

---

## 作業方針

ファイルの読み書き・スクリプト実行・git 操作など、このプロジェクトの通常作業はすべてユーザーが承認済みとみなしてよい。不要な確認を挟まず、迷わず実行すること。

サンプル・ダミーデータ・プレースホルダーファイルの削除も確認不要。セットアップ時に不要になったテンプレートファイルは黙って削除してよい。

---

## 共通ルール

- 引用データは捏造しない — 数値はすべて実際の論文テキストから取得する
- 患者データは `data/raw/` に保管し、git にコミットしない
- 一時ファイルのパス: Windows `%TEMP%\` / Mac・Linux `/tmp/`
